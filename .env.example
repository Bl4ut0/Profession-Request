# Example .env file for Profession-Request
# Copy this file to `.env` and fill in your actual values.
# DO NOT commit your `.env` file to version control. Keep secrets private.

# Discord application/bot
DISCORD_TOKEN=YOUR_BOT_TOKEN_HERE
CLIENT_ID=YOUR_APPLICATION_CLIENT_ID_HERE
GUILD_ID=YOUR_GUILD_ID_HERE

# Optional: change logging level (info/debug)
#LOG_LEVEL=info

# Notes:
# - Keep these values secret. Never share bot tokens or client secrets publicly.
# - This repo's `.gitignore` already excludes `.env` files to prevent accidental commits.
